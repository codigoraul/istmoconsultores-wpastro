---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import PageHero from '../components/PageHero.astro';
import Footer from '../components/Footer.astro';
import { getServicios } from '../lib/wordpress';

const servicios = await getServicios();
---

<Layout title="Servicios — Istmo Consultores">
  <Header />
  <main>
    <PageHero
      variant="edificios"
      title="Servicios"
      subtitle="Tres pilares estratégicos para proyectos sostenibles"
      image="/images/title-servicios.webp"
    />

    <section class="bg-white py-10 border-b border-gray-100">
      <div class="container">
        <p class="text-base text-gris leading-relaxed max-w-3xl">
          El trabajo de Istmo Consultores se estructura en tres pilares estratégicos, integrando análisis político, regulación, comunicación, desarrollo territorial y gestión de proyectos.
        </p>
      </div>
    </section>

    {servicios.map((svc, index) => {
      const ids = ['estrategia', 'innovacion', 'proyectos'];
      return (
      <section id={ids[index]} class={`relative overflow-hidden py-20 ${index % 2 === 0 ? 'bg-white' : 'bg-fondo'}`}>
        {index === 1 && (
          <div class="absolute inset-0 z-0">
            <img src="/images/bg-oficina-transparente.webp" alt="" class="w-full h-full object-cover object-center opacity-[0.06]" />
          </div>
        )}
        <div class="container relative z-10">
          <h2 class="text-2xl md:text-3xl font-medium text-negro leading-tight mb-8">{svc.titulo}</h2>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {svc.subareas.map(sub => (
              <div class="bg-white border border-gray-200 border-t-[3px] border-t-corp rounded p-6 flex flex-col gap-4">
                <h3 class="text-lg font-semibold text-negro leading-snug">{sub.titulo}</h3>
                <p class="text-[13px] text-gris leading-relaxed">{sub.descripcion}</p>
                <p class="text-[11px] font-semibold text-negro tracking-widest uppercase mt-2">Servicios</p>
                <ul class="flex flex-col gap-2 flex-1">
                  {sub.puntos.map(item => (
                    <li class="flex items-start gap-2 text-[13px] text-gris leading-snug">
                      <span class="w-1.5 h-1.5 rounded-full bg-medio flex-shrink-0 mt-1.5"></span>
                      {item}
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </section>
    );
    })}

    <section class="bg-corp py-16">
      <div class="container text-center">
        <h2 class="text-2xl md:text-3xl font-medium text-white mb-4">¿Necesitas asesoría estratégica?</h2>
        <p class="text-[15px] text-cielo leading-relaxed max-w-xl mx-auto mb-6">
          Conversemos sobre cómo podemos apoyar tu proyecto u organización.
        </p>
        <a href="/contacto/" class="btn-ghost">Contáctanos</a>
      </div>
    </section>
  </main>
  <Footer />
</Layout>
